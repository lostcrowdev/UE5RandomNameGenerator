# UE5RandomNameGenerator
A lightweight, data-driven random name generator component for Unreal Engine 5.7 that generates first, middle, and last names for NPCs or any actor at runtime, with optional gender filtering and fully customizable name lists via Data Tables.
